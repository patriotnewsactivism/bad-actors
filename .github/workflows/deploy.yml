name: Legacy Cloud Run deployment retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Cloud Run deployment retired
        run: |
          echo "Bad Actors production is migrating away from Google Cloud Run."
          echo "This workflow no longer deploys or changes Google infrastructure."
          echo "Use the canonical Vercel deployment after the Vercel project is imported and configured."
